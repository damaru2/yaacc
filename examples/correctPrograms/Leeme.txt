En el conjunto de estos ejemplo se prueba todo lo que hace el lenguaje.
Sin embargo si sólo se quiere ver en líneas generales el funcionamiento,
los ejemplos más ilustrativos son:

input3.c	While, if, else, break, continue
input6.c 	Factorial recursivo
input7.c	Factorial mutuamente recursivo (se usan prototipos)
input10.c	Uso de structs anidados
input17.c	For, declaración y uso de arrays compuestos locales y globales
input19.c	Arrays y structs recursivamente unos dentro de otros sin límite
input20.c	La asignación es una expresión más, se puede encadenar y operar con su resultado
input24.c	Los enteros se pueden usar como booleanos (a priori la máquina p no permite esto)
input25.c	Quicksort recursivo (ejecutar con unos 500 de pila)
